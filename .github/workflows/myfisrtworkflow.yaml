name: My fisrt Workflow

on:
 push:
 pull_request:
 workflow_dispatch:
 
jobs:
    myfirstjob:
              name: First Job
              runs-on: ubuntu-latest
              steps:
              - name: step one
                uses: actions/checkout@v3
              - name: step two
                run: |
                  touch newfileLC.yaml
                  echo "this is Luis Carranza" >> newfileLC.yaml
                  cat newfileLC.yaml
                  pwd
